{
  "name": "assignment-b3-pwd",
  "version": "1.1.0",
  "description": "1DV025 - Examination Assignment Module B - PWD",
  "type": "module",
  "browser": "src/js/index.js",
  "scripts": {
    "dev": "vite",
    "build": "mkdir dist && cp -r src/* dist/ && vite build",
    "serve": "vite preview",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0"
  },
  "contributors": [
    "Beata Eriksson <be222gr@student.lnu.se>"
  ],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.7",
    "vite": "^3.2.4"
  }
}
